---
name: brain-memory-session
description: "Handle session lifecycle: generate summary on session end"
metadata:
  openclaw:
    emoji: "📝"
    events: ["command:new"]
---

# Brain Memory Session Hook

Generates session summaries and clears working memory when user starts a new session with `/new` command.